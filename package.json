{
  "name": "web-scraper-project",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "pip3 install -r requirements.txt && python src/main.py",
    "postinstall": "pip3 install -r requirements.txt"
  },
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}